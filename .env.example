# OpenAI API Configuration
# Get your API key from https://platform.openai.com/api-keys
OPUS_OPENAI_API_KEY=your_openai_api_key_here

#If you plan on changing the name of the api key, make sure to change it everywhere else. This can easily be done by a search and replace in a code editor like Cursor or vscode
